{
  "comment": [
    "Compiles the fixture the way an external consumer would: it imports the",
    "package by its published specifiers ('zod-nostr' / 'zod-nostr/mini'),",
    "which the `paths` mapping resolves to the BUILT declarations in dist/ — not",
    "to src/. So this exercises the emitted .d.ts (the real public type surface),",
    "catching type regressions that source-relative tests can't. Requires",
    "`npm run build` first."
  ],
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "noEmit": true,
    "paths": {
      "zod-nostr": ["../../dist/classic.d.ts"],
      "zod-nostr/mini": ["../../dist/mini.d.ts"]
    }
  },
  "include": ["consumer.ts"]
}
