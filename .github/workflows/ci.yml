name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 26
          cache: npm

      - run: npm ci

      - name: Type check
        run: npm run typecheck

      - name: Lint & format check
        run: npm run check

      - name: Test
        run: npm run test:coverage

      - name: Spec baseline check (README vs. spec-baseline.json)
        run: npm run test:spec-baseline

      - name: Upload coverage reports to Codecov
        if: ${{ !cancelled() }}
        uses: codecov/codecov-action@v7
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage/lcov.info

      - name: Build
        run: npm run build

      - name: Public JSDoc check (built declarations)
        run: npm run test:jsdoc

      - name: External consumer compile (packed tarball)
        run: npm run test:consumer
